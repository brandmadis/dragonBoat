import React from 'react' 
import { config } from '../firebase'

const Home = () => {
    let key = process.env.REACT_APP_devApiKey
    let test = "test"
    return (
        <div>
            <p>Home</p>
        </div>
            
    )
} 

export default Home

